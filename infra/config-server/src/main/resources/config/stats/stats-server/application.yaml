server:
  port: 0

spring:
  application:
    name: stats-server
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ewm-stats
    username: postgres
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  jackson:
    serialization:
      INDENT_OUTPUT: true
logging:
  level:
    root: info
    org:
      zalando:
        logbook: trace

eureka:
  instance:
    hostname: localhost
    preferIpAddress: false
    instance-id: stats-server:${random.value}