server:
  port: 0

spring:
  application:
    name: event-service
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ewm_event?currentSchema=event_service
    username: postgres
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: ALWAYS
      schema-locations: classpath:schema.sql
  jackson:
    serialization:
      INDENT_OUTPUT: true
logging:
  level:
    root: info
    org:
      zalando:
        logbook: trace

grpc:
  client:
    analyzer:
      address: 'discovery:///analyzer'
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext
    collector:
      address: 'discovery:///collector'
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext

eureka:
  instance:
    hostname: localhost
    preferIpAddress: false
    instance-id: event-service:${random.value}